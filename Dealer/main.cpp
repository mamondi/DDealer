#include "Client.h"
#include "DealerOffice.h"
int main() {
    DealerOffice dealerOffice;
    Client client;

    PackageDTO packageDTO = dillerOffice.createPackageDTO();

    client.sendPackage(packageDTO);

    return 0;
}
